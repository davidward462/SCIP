(define (inc x) (+ x 1))

(define pi 3.1415)

(define (square x) (* x x))

(define (inv x) (/ 1 x))

(define (lbs-to-kg lbs) (/ lbs 2.2))

(define (kg-to-lbs kg) (* kg 2.2))

(define (area-rect l w) (* l w))

(define (perim-rect l w) (+ l l w w))
